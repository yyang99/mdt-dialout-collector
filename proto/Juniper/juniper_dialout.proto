syntax = "proto3";
import "juniper_gnmi.proto";

// package Nokia.SROS;

service Subscriber {
  rpc DialOutSubscriber(stream gnmi.SubscribeResponse) returns (stream PublishResponse);
}
//message PublishResponse {}
